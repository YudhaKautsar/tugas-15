const ab = 8
const bc = 6

const ac = Math.pow(ab,2) + Math.pow(bc,2);

console.log(`Nilai AB : ${ab}`);
console.log(`Nilai BC : ${bc}`);
console.log(`panjang sisi AC pada segitiga siku-siku tersebut adalah : ${Math.sqrt(ac)} cm`);